[package]
name = "multithread"
version = "0.1.0"
edition = "2024"
publish = ["artifactory"]

[dependencies]
error = { path = "../error" } 
common = { path = "../common" } 
rayon = "1.11.0"
reqwest = { workspace = true }
serde =  { workspace = true }
trust-dns-resolver = { workspace = true }
